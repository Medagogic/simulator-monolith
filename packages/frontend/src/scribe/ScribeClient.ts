/* eslint-disable */
    /**
    * This file was automatically generated by Scribe's codegen tool.
    * This class was generated by GPT3.5, so godspeed and good luck.
    */

    import { Socket } from "socket.io";
    import { ChatMessage, SimUpdateData } from "./scribetypes";

export abstract class ScribeClient {
  socket: Socket;
  abstract on_chat_message(data: ChatMessage): void;
  abstract on_patient_state(data: SimUpdateData): void;

  constructor(socket: Socket) {
    this.socket = socket;
    this.socket.on("chat_message", (data: ChatMessage) => this.on_chat_message(data));
    this.socket.on("patient_state", (data: SimUpdateData) => this.on_patient_state(data));
  }
}